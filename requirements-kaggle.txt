# Kaggle-Optimized Requirements
# Fix numpy compatibility issues by upgrading numpy first
# Then install other packages

# IMPORTANT: Install in this order on Kaggle:
# 1. pip install --upgrade numpy
# 2. pip install -r requirements-kaggle.txt

# Fix numpy compatibility (install this FIRST)
numpy>=1.24.0

# Core Deep Learning (usually pre-installed, but specify versions for compatibility)
torch>=2.0.0
torchvision>=0.15.0

# Configuration Management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Experiment Tracking (offline mode, no API key needed)
wandb>=0.15.0

# XAI Libraries
captum>=0.6.0
grad-cam>=1.4.8

# Utils
pyyaml>=6.0
tqdm>=4.65.0

# Visualization
plotly>=5.14.0

# Optional: Uncomment if needed for specific experiments
# shap>=0.42.0  # For SHAP explanations
# umap-learn>=0.5.3  # For manifold learning
# transformers>=4.30.0  # For LLM integration
# openai>=1.0.0  # For OpenAI API
# uncertainty-toolbox>=0.1.0  # For uncertainty quantification
