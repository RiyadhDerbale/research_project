# Manifold Analysis Configuration

task: classification
model_path: experiments/checkpoints/best_model.pth

model:
  name: simple_cnn
  params:
    in_channels: 3

data:
  num_classes: 10
  image_size: 32

manifold:
  method: umap # or 'pca'
  n_components: 2
  build_index: true

num_samples: 500
output_dir: experiments/manifold_plots
device: null
seed: 42
