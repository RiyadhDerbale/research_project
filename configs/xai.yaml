# XAI Configuration

task: classification # or segmentation
model_path: experiments/checkpoints/best_model.pth

model:
  name: simple_cnn
  params:
    in_channels: 3

data:
  num_classes: 10
  image_size: 32

xai:
  methods:
    - integrated_gradients
    - gradient_shap
    - saliency

num_samples: 10
output_dir: experiments/xai_outputs
device: null
seed: 42
